const { prisma } = require("./db");

const readLeaderboardByPuzzleId = async (puzzleId) => {};

const createPlayer = async () => {};

const updatePlayerById = async (playerId) => {};

module.exports = {
  readLeaderboardByPuzzleId,
  createPlayer,
  updatePlayerById,
};
